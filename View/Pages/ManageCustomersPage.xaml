<Page x:Class="Bookmaster34.View.Pages.ManageCustomersPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Bookmaster34.View.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="1004" d:DesignWidth="1920"
     Title="Читатели" Background="White">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="400"/>
            <ColumnDefinition Width="20"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!--Панель поиска-->
        <Border Background="{DynamicResource Grey}">

            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="10"/>
                    <RowDefinition Height="50"/>
                    <RowDefinition Height="720"/>
                    <RowDefinition Height="50"/>

                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="1*"/>
                </Grid.ColumnDefinitions>



                <TextBlock Grid.Row="2" x:Name="CustomerIdTb" Text="ID Читателя"/>
                <TextBox Grid.Row="2"  Grid.Column="2"/>

                <TextBlock Grid.Row="4" x:Name="CustomerNameTb" Text="Имя"/>
                <TextBox Grid.Row="4" Grid.Column="2"/>



                <Button Name="SearchBtn"  Grid.Row="6" Grid.ColumnSpan="3"
                 Content="Поиск" Click="SearchBtn_Click"  />

                <Grid Grid.Row="8" Grid.ColumnSpan="3" Margin=" 0 0 0 0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="170" />
                        <ColumnDefinition Width="20" />
                        <ColumnDefinition Width="170" />
                    </Grid.ColumnDefinitions>
                    <Button Grid.Column="0" Content="Редактировать"   Name="EditBtn" Click="EditBtn_Click" />

                    <Button Grid.Column="2" Content="Добавить" Name="AddBtn" Click="AddBtn_Click" />
                </Grid>

            </Grid>

        </Border>

        <Grid Grid.Column="2">
            

            <!--Результаты поиска-->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1*"/>
                    <RowDefinition Height="5"/>
                    <RowDefinition Height="50"/>
                </Grid.RowDefinitions>


                <ListView Name="CustomerLv" SelectionChanged="CustomerLv_SelectionChanged">

                    <!-- Создаем заголовки таблицы-->
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="ID" DisplayMemberBinding="{Binding Id}"/>

                            <GridViewColumn Header="Имя" DisplayMemberBinding="{Binding Name}"/>

                            <GridViewColumn Header="Адрес" DisplayMemberBinding="{Binding Address}"/>

                            <GridViewColumn Header="Почтовый индекс" DisplayMemberBinding="{Binding Zip}"/>

                            <GridViewColumn Header="Город" DisplayMemberBinding="{Binding City.Name}"/>
                        </GridView>
                    </ListView.View>
                </ListView>

            </Grid>

        </Grid>

    </Grid>

</Page>
